{
  "name": "mentor-match-clean",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n BACK,FRONT -c green,cyan \"cd backend && pnpm run dev\" \"cd frontend && pnpm run dev\"",
    "build": "pnpm -r run build",
    "start": "node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/multer": "^1.4.12",
    "@types/node": "^20.11.24",
    "@types/selenium-webdriver": "^4.1.28",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "multer": "1.4.5-lts.2"
  }
}